export type CommentFeedStyleProps = {
  backgroundColor: string;  // Background color of the comment feed
  commentsGap: number;      // Space between individual comments (for threaded, usually 0)
};
